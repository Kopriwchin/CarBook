<%- include('../partials/header') %>

<div class="max-w-6xl mx-auto px-4 py-8">
    <div class="text-center mb-12">
        <h1 class="text-3xl font-bold text-gray-900">Магазин за услуги</h1>
        <p class="text-gray-500 mt-2">Разширете капацитета на вашия дигитален гараж.</p>
        <div class="mt-4 inline-block bg-blue-50 px-4 py-2 rounded-lg text-blue-700 font-medium">
            Текущ лимит: <%= user.carLimit %> автомобила
        </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        <% products.forEach(product => { %>
            <div class="bg-white rounded-xl shadow-lg border border-gray-100 overflow-hidden hover:shadow-xl transition-shadow duration-300">
                <div class="p-8">
                    <div class="w-12 h-12 bg-indigo-100 rounded-lg flex items-center justify-center mb-6 text-indigo-600">
                        <i class="fas <%= product.icon %> text-2xl"></i>
                    </div>
                    <h2 class="text-2xl font-bold text-gray-900 mb-2"><%= product.name %></h2>
                    <p class="text-gray-500 mb-6 min-h-[50px]"><%= product.description %></p>
                    
                    <div class="flex items-end mb-6">
                        <span class="text-4xl font-bold text-gray-900"><%= product.price.toFixed(2) %></span>
                        <span class="text-gray-500 ml-1 mb-1">лв.</span>
                    </div>

                    <a href="/checkout/<%= product.id %>" class="block w-full bg-indigo-600 text-white text-center py-3 rounded-lg font-bold hover:bg-indigo-700 transition">
                        Купи сега
                    </a>
                </div>
                <div class="bg-gray-50 px-8 py-4 border-t border-gray-100">
                    <ul class="text-sm text-gray-600">
                        <li class="flex items-center mb-2">
                            <i class="fas fa-check text-green-500 mr-2"></i>
                            +<%= product.slots %> слот(а) за автомобили
                        </li>
                        <li class="flex items-center">
                            <i class="fas fa-check text-green-500 mr-2"></i>
                            Вечна валидност
                        </li>
                    </ul>
                </div>
            </div>
        <% }) %>
    </div>
</div>

<%- include('../partials/footer') %>